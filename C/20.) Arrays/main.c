#include <stdio.h>

int main()
{
    // Arrays are data structures that can store many values of the same data type

    double prices[4] = {5.00, 4.67, 10.44, 20.00};

    printf("$%.2lf", prices[2]);

    return 0;
}
